agent:
  name: deep-research-plan-array-agent
  version: 0.1.0
  description: "Plan a bounded array of research topics from a user question."

inputs:
  question: string

outputs:
  plan:
    type: object
    required: [research_brief, topics]
    properties:
      research_brief: string
      topics:
        type: array
        min_items: 4
        max_items: 4
        items: string

state:
  phase:
    type: enum
    enum: [init, plan, done, failed]
  plan:
    type: object
    required: [research_brief, topics]
    properties:
      research_brief: string
      topics:
        type: array
        min_items: 4
        max_items: 4
        items: string

workflow:
  kind: state_machine
  initial: init
  max_steps: 5
  terminal: [done, failed]
  rules:
    - id: init
      when: phase == "init"
      actions:
        - kind: set
          values:
            phase: plan
    - id: plan
      when: phase == "plan"
      actions:
        - kind: model_call
          model: research_planner_array
          input:
            object:
              question:
                ref: question
              max_topics:
                literal: 4
          output: plan
          timeout_seconds: 60
          retry:
            max_attempts: 3
        - kind: set
          values:
            phase: done
    - id: done
      when: phase == "done"
      actions:
        - kind: return
          output: plan

policy:
  timeout_seconds: 90

observability:
  traces: true
