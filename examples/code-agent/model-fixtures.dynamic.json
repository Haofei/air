{
  "fixtures": {
    "code_tool_planner": {
      "$sequence": [
        {
          "complete": false,
          "tool_calls": [
            {
              "tool": "repo.search",
              "input": {
                "query": "tool_batch_dispatch",
                "mode": "fixed",
                "max_matches": 20
              }
            },
            {
              "tool": "repo.context",
              "input": {
                "query": "ToolBatchDispatch",
                "mode": "fixed",
                "context_lines": 3,
                "max_matches": 5
              }
            }
          ],
          "rationale": "Search for the governed batch dispatch action and gather nearby implementation context before summarizing how the kernel handles it."
        },
        {
          "complete": true,
          "tool_calls": [],
          "rationale": "The first batch observation is enough for the fixture run; summarize the bounded dispatch behavior."
        }
      ]
    },
    "code_dynamic_summarizer": {
      "summary": "Fixture dynamic exploration completed: the model selected a bounded read-only tool batch, and AIR executed only declared tools under capability, budget, and repeated-call checks.",
      "relevant_files": [
        {
          "path": "crates/air-core/src/lib.rs",
          "reason": "Defines governed dynamic dispatch actions as part of the AIR IR."
        },
        {
          "path": "crates/air-runtime/src/lib.rs",
          "reason": "Executes model-selected tool batches through the same governed path as static tool_call."
        },
        {
          "path": "crates/air-verify/src/lib.rs",
          "reason": "Verifies dynamic dispatch inputs, outputs, timeouts, retries, bounds, and approval-path requirements."
        }
      ],
      "findings": [
        {
          "title": "Dynamic tool batches stay inside AIR contracts",
          "evidence": "tool_batch_dispatch accepts typed {tool, input} choices, but runtime still rejects undeclared tools, mismatched capabilities, over-budget calls, repeated identical calls, schema-invalid outputs, and unsafe phase writes.",
          "source_ids": []
        }
      ],
      "source_ids": [],
      "next_steps": [
        "Use this pattern to replace fixed read-only gathering stages with a bounded plan-act-observe batch loop.",
        "Keep write tools out of dynamic dispatch until a module has an explicit approval path and narrow patch policy."
      ]
    }
  }
}
