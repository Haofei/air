store:
  name: review-agent-store
  version: 0.1.0

imports:
  - ../../modules/std/module-store.air-store.yaml

modules:
  review.agent@0.1.0:
    path: skills/review-agent/review-loop.air.yaml
    kind: composite
    visibility: public
    description: "Read-only review-agent loop using search, bounded reads, skill instructions, web fetch, and read-only GitLab MCP tools."
    tags: [review, code_review, read_only, mcp]
    covers: [code_review, diff_review, merge_request_review]
    priority: 80
