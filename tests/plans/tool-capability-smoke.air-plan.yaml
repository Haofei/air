plan:
  name: tool-capability-smoke
  version: 0.1.0

requires:
  capabilities: [retrieval.local]

nodes:
  - id: search
    module: test.tool_capability_smoke@0.1.0

entry: search

connect:
  - from: $input.text
    to: search.text

outputs:
  result: search.result
